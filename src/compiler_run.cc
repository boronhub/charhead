#include "compiler.hpp"

int main() {
  CharHead ch;

  ch.CodeCompiler(ch.ReadFile("input_one"));

/*   std::cout << "Input CharHead Code: ";
  char str[1024];
  std::cin.get(str, 1024);
  ch.CodeCompiler(str); */
}